// namespace Playground.Archive;
//
// public class MonoTorrent
// {
//     
// }

/*
        var torrent = await MonoTorrent.Torrent.LoadAsync(FilePath);
        var settings = new EngineSettingsBuilder
        {
            AllowLocalPeerDiscovery = true,
            
            AutoSaveLoadDhtCache = true, //TODO: Read about DHT
            
            AutoSaveLoadFastResume  = false,
            
            CacheDirectory = @"E:\src\Bogers.Sofie\.torcache",
            
            DiskCacheBytes = 0, //Disabled
            
            ListenPort = -1, //Disabled
        }.ToSettings();

        using var engine = new ClientEngine(settings);
        var pieceRequester = new StreamingPieceRequester();
        
        
        engine.
*/